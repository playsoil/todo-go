to run project

make a copy of .env.example and fill values.

```bash
cp .env.example .env
```

then:

For Linux/Mac users:

```
make up
```

windows user:

```bash
docker compose up
```

Then go to:  
[localhost:9120](http://localhost:8000)
